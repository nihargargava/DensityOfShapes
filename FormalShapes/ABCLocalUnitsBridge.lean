import main

/-!
# Compatibility import for the shifted Proposition 4.4 statements

The shifted exponent-lattice bridge now lives in `main.lean`, immediately
after Lemma 5.1, so that the final construction can use it without an import
cycle.  This module is retained as a compatibility import for the standalone
development files.
-/
